@model IEnumerable<ProjektniZadatak.Models.Atributiartikla>

@{
    ViewData["Title"] = "Atributi artikla";
}

<h1 class="text-center">Atributi artikla</h1>
<br />

<table class="table table-bordered table-dark text-center">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Vrijednostatributa)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Artikal)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Vrstaatributa)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Vrijednostatributa)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Artikal.Artikalid)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Vrstaatributa.Vrstaatributaid)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.Artikalid,id2=item.Vrstaatributaid }) |
                    @Html.ActionLink("Details", "Details", new { id=item.Artikalid,id2=item.Vrstaatributaid }) |
                    @Html.ActionLink("Delete", "Delete", new { id=item.Artikalid,id2=item.Vrstaatributaid })
            </td>
        </tr>
}
    </tbody>
</table>

<p>
    <a asp-action="Create">Dodaj novi atribut</a>
</p>